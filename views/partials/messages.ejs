<% if(typeof errors != 'undefined') { %>
	<% errors.forEach(function(error) { %>
		<p style="text-decoration: underline; font-weight: bold; border: none; border-radius: 5px; background: #C1C3D7; padding: 2px; padding-left: 5px;"> <%= error.msg %>! </p>	
	<% }) %>
<% } %>

